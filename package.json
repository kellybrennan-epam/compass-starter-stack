{
  "name": "compass-starter-stack-content",
  "version": "2.3.0",
  "description": "This repository aims to provide sample stack content to users for the implementation of compass starter app demo websites. An easy way to import this stack content is by using the seed command.",
  "engines": {
    "node": ">=18.17.0 <=22.6.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {},
  "devDependencies": {}
}
